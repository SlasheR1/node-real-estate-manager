<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
     <link rel="stylesheet" href="/css/style.css">
     <!-- Можно добавить еще стили для главной страницы -->
</head>
<body>
    <%- include('partials/header') %> <!-- Включаем шапку -->
    <%- include('partials/messages') %> 
    <main class="container">
        <h1>Добро пожаловать в Менеджер Недвижимости!</h1>

        <% if (currentUser) { %>
            <p>Привет, <strong><%= currentUser.username %></strong>!</p>
            <p>Перейти к <a href="/properties">списку объектов</a>.</p>
        <% } else { %>
            <p>Для просмотра объектов, пожалуйста, <a href="/login">войдите</a> или <a href="/register">зарегистрируйтесь</a>.</p>
        <% } %>

    </main>

    <%- include('partials/footer') %> <!-- Включаем подвал -->
</body>
</html>